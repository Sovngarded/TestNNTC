# TestNNTC

Тестовое задание. 
```diff
Два брейкпоинта в контроллере доступы по следующим ссылкам:
+ https:// ... /Catalogue/DownLoadFile  // Выводит текстовый файл со всеми категориями и стоимостью, а так же сумма стоимости каждого из элементов и каждого второго.
+ https:// ... /Catalogue/DownLoadEveryTwoFile  // Выводит текстовый файл со всеми родительскими категориями, но в каждой 2 дочерних, а так же сумма стоимости каждого из элементов и каждого второго.
```
